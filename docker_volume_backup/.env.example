# Example .env file for docker_volume_backup.sh
# Copy this file to .env and fill in your own values

# Path to the backup directory (must be writable)
BACKUP_DIR="/your/backup/location"

# Name of the Docker volume to back up
DOCKER_VOLUME="your-docker-volume-name"

# Prefix for the backup file name (e.g., 'myvolume_backup')
BACKUP_NAME="your-backup-name-prefix"

# Number of backups to keep (older backups will be deleted)
BACKUP_KEEP_COUNT=7

# Date format for backup file names (default: %d-%m-%Y)
BACKUP_DATE_FORMAT="%d-%m-%Y"